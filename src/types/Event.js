import "./InnerComponent.js";

/**
 * @typedef {Object} Event
 * @property {string} name
 * @property {Array<InnerComponent>} subscribers
 * @property {Array<InnerComponent>} emitters
 * @property {function} emit
 */
