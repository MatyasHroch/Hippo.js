import "../types/InnerComponent.js";
/**
 *
 * @param {InnerComponent} component
 * @param {Array<string>} properties
 */

function assignProperties(parentComponent, childComponent) {}
/**
 * assign propertie from parent to child
 * @param {InnerComponent} component
 * @param {string} propertie
 */
function assignPropertie(parentComponent, childComponent) {}

export { assignProperties, assignPropertie };
