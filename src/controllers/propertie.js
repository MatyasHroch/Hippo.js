import InnerComponent from "../types/InnerComponent.js";

/**
 *
 * @param {InnerComponent} component
 * @param {Array<string>} properties
 */

function assignProperties(component, properties) {}
/**
 * assign propertie from parent to child
 * @param {InnerComponent} component
 * @param {string} propertie
 */
function assignPropertie(component, propertie) {}
